# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
.next/
out/
build/
dist/

# production
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*
!.env.example

# misc
.DS_Store
*.pem
.vscode/
.idea/

# firebase
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
deploy-output.log
# service account 키 — 절대 커밋 금지
firebase-adminsdk-*.json
*-service-account.json
service-account*.json
gcloud-key*.json

# original prototype assets (keep but don't ship)
asset/
CLAUDE.md
# 프로젝트 문서 — 로컬 SSOT, 깃에 올리지 않음
docs/
# attract 영상 — GCS 공개 버킷에서 직접 서빙 (config.VIDEO_BASE_URL)
# git repo 와 분리해 코드 푸시와 영상 교체가 독립적으로 동작.
public/videos/
